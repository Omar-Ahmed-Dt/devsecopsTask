# Task 1 - Bash Scripting: Service Inventory Health Report

## How to run
```bash
./service_report.sh inventory.txt > stdout.txt 2> stderr.txt
```

The command above runs the script using `inventory.txt`, sends normal report output to `stdout.txt`, and sends warnings/summary output to `stderr.txt`.
